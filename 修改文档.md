# 修改文档

## CabinetTask 放置稳定性优化

### 修改位置

- `rocobench/envs/task_cabinet.py`
- `prompting/parser.py`
- `rocobench/policy.py`
- `run_dialog.py`

### 修改内容

在 `CabinetTask` 中启用 cabinet 专用的 pick-and-place 预放置和慢释放参数：

```python
@property
def use_preplace(self):
    return True

@property
def use_pick_place_preplace(self):
    return True

@property
def pick_place_preplace_height(self):
    return 0.16

@property
def slow_release_policy_kwargs(self):
    return dict(
        release_control_freq=1,
        post_release_hold_steps=30,
        slow_release_objects=("cup",),
    )
```

同时让 parser 在环境声明 `use_pick_place_preplace=True` 时，把 `PICK cup PLACE cup_coaster` 这类合并动作拆成：

1. `PICK cup`
2. 移动到 `cup_coaster` 上方预放置点
3. 从预放置点缓慢下降到最终放置点并释放

`policy.py` 新增默认关闭的慢释放参数。只有 `run_dialog.py` 从当前环境读到 `slow_release_policy_kwargs` 时才会启用，因此目前只对 `CabinetTask` 的 `cup` 释放生效。

### 修改原因

在 cabinet 任务日志中，杯子放到 `cup_coaster` 后存在接触不稳定、释放后滑落的情况。原始放置流程会直接移动到最终放置点并释放，随后机械臂立即返回 home，容易在释放瞬间给杯子带来不稳定接触或残余速度。

之前只启用 `use_preplace` 不足够，因为 cabinet 的动作格式是合并动作 `PICK cup PLACE cup_coaster`，原 parser 的 `parse_pick_and_place()` 没有使用 `use_preplace`。因此这次补上专门针对合并 pick-and-place 的预放置逻辑，并对 cup 的最终释放段做慢速执行。

现在 cup 的放置过程会：

1. 先移动到目标 coaster 上方约 `0.16m` 的预放置点。
2. 再用更密集的控制点从预放置点下降到最终放置点。
3. 释放 cup 后原地保持 30 个控制步，再返回 home。

这样可以让 cabinet 任务中的 mug/cup 放置过程更平稳，降低杯子释放后滑落的概率。

### 修改范围

该修改只在 `CabinetTask` 类中生效，不影响其他任务：

- 不影响 `sort`
- 不影响 `rope`
- 不影响 `sweep`
- 不影响 `sandwich`
- 不影响 `pack`

虽然 `parser.py` 和 `policy.py` 是共享模块，但新增逻辑默认关闭。只有环境显式提供 `use_pick_place_preplace=True` 和 `slow_release_policy_kwargs` 时才启用。

### 合规说明

本修改没有改变任务目标、成功判定或物理环境参数：

- 未修改 `align_threshold`
- 未修改 coaster 位置
- 未修改 mug/cup 初始位置
- 未修改物体质量、摩擦、重力或碰撞体
- 未修改 reward/done 判定逻辑

该修改仅改变 cabinet 任务中 cup 的执行轨迹节奏：更高处预放置、更慢下降、释放后短暂停留，属于执行稳定性优化。
