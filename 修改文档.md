# 修改文档

## CabinetTask 放置稳定性优化

### 修改位置

- `rocobench/envs/task_cabinet.py`

### 修改内容

在 `CabinetTask` 类中覆盖 `use_preplace` 属性：

```python
@property
def use_preplace(self):
    return True
```

### 修改原因

在 cabinet 任务日志中，杯子放到 `cup_coaster` 后存在接触不稳定、释放后滑落的情况。原始放置流程会直接移动到最终放置点并释放，随后机械臂立即返回 home，容易在释放瞬间给杯子带来不稳定接触或残余速度。

启用 `use_preplace` 后，已有 parser 逻辑会将放置拆成两段：

1. 先移动到目标 coaster 上方约 `0.1m` 的预放置点。
2. 再从预放置点下降到最终放置点并释放物体。

这样可以让 cabinet 任务中的 mug/cup 放置过程更平稳，降低杯子释放后滑落的概率。

### 修改范围

该修改只在 `CabinetTask` 类中生效，不影响其他任务：

- 不影响 `sort`
- 不影响 `rope`
- 不影响 `sweep`
- 不影响 `sandwich`
- 不影响 `pack`

### 合规说明

本修改没有改变任务目标、成功判定或物理环境参数：

- 未修改 `align_threshold`
- 未修改 coaster 位置
- 未修改 mug/cup 初始位置
- 未修改物体质量、摩擦、重力或碰撞体
- 未修改 reward/done 判定逻辑

该修改仅启用已有的预放置路径机制，属于针对 cabinet 任务的执行轨迹稳定性优化。
